[pytest]
# The modules under test live in src/, the suite lives in tests/. Putting src/
# on sys.path here is what lets tests say `import preformulation` without the
# project having to be packaged and installed.
pythonpath = src
testpaths = tests
