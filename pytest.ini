[pytest]
# src/ is a package; the repo root on sys.path is what lets tests say
# `import src.data_processing.preformulation` without the project having to be
# packaged and installed.
pythonpath = .
testpaths = tests
